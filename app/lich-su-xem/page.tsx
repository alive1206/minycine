import type { Metadata } from "next";
import { WatchHistoryPage } from "@/containers/watch-history";

export const metadata: Metadata = {
  title: "Xem Tiếp — MinyCine",
  description: "Tiếp tục xem phim đang dở trên MinyCine.",
};

export default WatchHistoryPage;
