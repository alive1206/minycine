import type { Metadata } from "next";
import { LatestMoviesPage } from "@/containers/latest";

export const metadata: Metadata = {
  title: "Phim Mới Cập Nhật — MinyCine",
  description: "Danh sách phim mới cập nhật mới nhất tại MinyCine. Xem ngay!",
};

export default LatestMoviesPage;
